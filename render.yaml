services:
  - type: web
    name: elementiq-pro-backend
    runtime: node
    buildCommand: npm install
    startCommand: npx tsx server.ts
    envVars:
      - key: NODE_ENV
        value: production 
